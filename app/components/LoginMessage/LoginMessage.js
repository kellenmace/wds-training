import React from 'react';

const LoginMessage = () =>
	<div className="login-message">
		<p>Please <a href={WDSTTrainingData.loginURL} title="Log In">log in</a> to manage trainings.</p>
	</div>

export default LoginMessage;